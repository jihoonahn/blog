import Plot
import Publish

extension Node where Context == HTML.BodyContext {
    
    static func aboutPage(for page: Page, on site: Blog) -> Node {
        return .article(
            .header(
                .div(
                    
                )
            ),
            .div(
                
            )
        )
    }
    
}
